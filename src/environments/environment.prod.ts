const link = "http://46.253.132.73:9010/api/"

export const environment = {
  production: true,
  loggin: link + "users/loggin/",
  projects: link + "projects/",
  clients: link + "clients/",
  servicies: link + "servicies/",
  queries: link + "queries/",
  download: link + "download/"
};
